<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<p><%= link_to "Back to tasks", tasks_path %></p>
<p><%= link_to "Edit", edit_task_path(@task) %></p>
<p><%= link_to "Delete", delete_task_path(@task), method: :delete, data: { confirm: "Are you sure?" } %></p>
